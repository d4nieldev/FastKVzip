# Retention-score plots

User request (verbatim):

> In additions to the tables we have in the appendix with all the numbers I want to have plots for all the benchmarks where the x axis is the retention ratio and the y axis is the score, also include a straight dashed line which is the full cache score for comparison. Each plot should have 3 lines for the 3 methods. In addition to the per-benchmark-task plots also make plots for entire benchmarks like one for scbench, one for ruler-4k, etc..
> If in the official papers for the benchmarks there is a clear separation also do these plots.
> These are a lot of plots so make sure that everything is organized and tidy

Implementation scope (agent interpretation, not a separately approved plan): retain the tables, plot every configuration with matched three-method scores, reproduce benchmark aggregates, verify official task families from the papers, and deliver a clearly indexed appendix and standalone atlas. Use absolute scores matching the tables and each method's own full-cache baseline, consistent with the earlier user confirmation about baseline identity. Validate values, coverage, references, navigation, and rendered layout.
